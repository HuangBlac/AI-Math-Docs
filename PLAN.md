# LTFP 学习工具：历史设计契约

> 归档日期：2026-09-21。本文件由早期“前九章可验证学习系统修复与落地”计划压缩而来，只保留仍能约束实现的设计决定。它不是当前学习排程、完成报告或掌握证明。

## 当前事实从哪里读取

- 项目结构、安装和常用命令：`README.md`。
- 协作边界与文档路由：`AGENTS.md`、`CLAUDE.md`。
- 教材路径、锁定哈希和模型配置：`study.toml`。
- LTFP 笔记覆盖：`docs/computation/ltfp/index.md` 与 `study-map.md`。
- 笔记导入规则：根目录 `NOTE-IMPORT.md`。
- 工具实际能力：`src/ai_math_study/`、`tests/` 与当前 CLI 帮助。

上述当前证据优先于本文件。旧版计划、评审轮次、截止日期和阶段估时可从 Git 历史恢复，但不再保留为活跃项目说明。

## 仍然有效的设计决定

### 1. 来源权威与核验状态分离

- `study.toml` 指向的锁定教材及其 SHA-256 是教材内容的最高权威。
- PDF 抽取文本只用于定位；公式、页码、特殊符号和题目难度标记需要版面核对。
- 教材、人工核验材料、用户笔记和 AI 整理必须保留来源与核验状态。
- `note_unverified` 只说明有笔记材料，不表示已经掌握或通过验证。

### 2. Corpus 与学习状态分离

- Corpus 按 generation 构建，生成物不可变并可由锁定输入重建。
- 学习状态独立保存，采用追加事件表达尝试、更正和人工复核。
- 重建索引不能覆盖历史作答；来源漂移时进入复核，不能自动迁移成绩。
- 来源层级、权威、核验状态、证据类型和公式不确定性是不同维度，不能合并成单一布尔值。

### 3. 证据包必须可追溯

- 检索、出题和判分引用固定 generation 中的证据，并校验内容哈希。
- 合法证据 ID 不等于语义相关；评分项需要明确的支撑关系。
- 证据不足、公式不确定、引用漂移或 grader 分歧时 fail closed，转人工复核。
- 模型评分默认是 provisional，不直接推进 mastery 或释放完整答案。

### 4. Provider 能力显式配置

- Provider、模型、thinking、reasoning effort、超时、重试和预算按 profile 配置，不在代码中隐式猜测。
- `LocalOnlyProvider` 必须在结构上禁止网络访问。
- 在线调用共享总次数、deadline 和费用预算；认证、预算、拒绝或非法输出按稳定错误处理。
- 外发只发送最小必要片段；疑似密钥、PII 或数学证据被脱敏规则命中时转人工复核。

### 5. 笔记处理以内容守恒为先

- `format` 只能进行可证明安全的格式编辑；无法证明内容与保护区不变时拒绝修改。
- `organize` 生成本地 proposal，不自动写回 tracked 笔记。
- 代码、公式、HTML、远程 URL、snippet/include 和其他主动构造需要额外保护。
- 发布新文件使用 no-replace 语义；不能可靠保证原子发布时 fail closed。

### 6. 私有材料与公开站点分离

- `.study/` 保存 corpus、状态、模型结果和学习记录；`output/`、`_ingest/` 保存本地转录和审计材料。
- 公开站点只接收包含学习知识本身的正文笔记；独立导入记录、逐页去向和整理决策不进入 `docs/` 或导航。
- 教材原件、逐页渲染、答案、模型输出和个人绝对路径不自动进入 Git 或 MkDocs。
- 提交、推送、构建和部署是四个独立动作，各自需要与用户授权和实际结果一致。

## 当前模块边界

`src/ai_math_study/` 当前包含配置、摄取、检索、状态、导学骨架、出题、判分、笔记处理和验证模块。模块存在不等于历史计划中的全部验收门已经通过；具体行为以代码、测试和当前命令输出为准。

常用验证命令：

```bash
uv sync --frozen
uv run ruff check src tests
uv run mypy src
uv run pytest -q
```

MkDocs 站点使用另行安装了 `mkdocs-material` 与 `pymdown-extensions` 的解释器验证：

```bash
python -m mkdocs build --strict
```

文档构建通过不能替代学习工具测试，工具测试通过也不能证明笔记内容正确或已经掌握。

## 已移除的过期内容

以下内容曾用于 2026 年 7–9 月的规划，现已从活跃文档中删除：

- 2026-07-14、2026-07-18 和 2026-09-05 等截止日期；
- “八周、每周二十小时”和 32 个学习日排程；
- 把 154/96 题清单当作当前承诺的表述；
- Phase 0、Gate B 和逐周交付顺序；
- 特定时期的 CLI 版本、评审线程、模型可用性和 Git ahead/未提交快照；
- 已完成的五轮计划评审日志。

需要重新规划学习或工程工作时，应基于当前代码、覆盖表和用户当次目标新建带日期的短计划，而不是复活上述旧时间表。
